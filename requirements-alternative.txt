# Alternative requirements.txt with pre-built wheels
# Use this if the main requirements.txt still fails

Flask>=3.0.0
Pillow>=10.0.0
python-dotenv>=1.0.0
google-generativeai>=0.3.0
Werkzeug>=3.0.0
gunicorn>=21.0.0

# These versions use pre-built wheels when possible
# Avoiding source compilation issues